
<% if signed_in? and !answer.author?(current_user) %>
  
	<% unless current_user.voted?(answer) %>
		<%= render "partials/create_votes", answer: answer %>
	<% else %>
		<%= render "partials/edit_votes", answer: answer %>
	<% end %>

<% end %>


